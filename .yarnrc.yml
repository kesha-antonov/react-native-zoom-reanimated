# React Native tooling (Metro, the Expo CLI, ESLint's config loader) expects a
# real node_modules tree. Yarn 3 defaults to Plug'n'Play, so pin the linker
# here rather than relying on a machine-level ~/.yarnrc.yml.
nodeLinker: node-modules
